import { SelectionParameters } from './selection-parameters';

describe('SelectionParameters', () => {
  it('should create an instance', () => {
    expect(new SelectionParameters()).toBeTruthy();
  });
});
